import 'package:flutter/material.dart';

class PraktisiView extends StatelessWidget {
  const PraktisiView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Praktisi view"));
  }
}
